# Swiper 9 → 14 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Bump `swiper` to v14 and update the one import path it broke, with the testimonials carousel verified working.

**Tech Stack:** React 19, Vite 8, Swiper 14, bun. Design: `docs/superpowers/specs/2026-08-29-swiper-v14-design.md`.

---

## Task 1: Upgrade + fix the import

**Files:**

- Modify: `package.json`, `bun.lock` (via `bun add`)
- Modify: `src/components/Testimonials.jsx`

- [ ] **Step 1: Bump the dependency**

```bash
bun add swiper@latest
```

Expected: `package.json` shows `"swiper": "^14.x"`; `bun.lock` updates.

- [ ] **Step 2: Fix the module import**

In `src/components/Testimonials.jsx`, line 2:

```diff
- import { Autoplay, Navigation, Pagination } from "swiper"
+ import { Autoplay, Navigation, Pagination } from "swiper/modules"
```

Leave lines 3 and 7–9 (`swiper/react`, `swiper/css`, `swiper/css/pagination`,
`swiper/css/navigation`) exactly as they are.

- [ ] **Step 3: Build**

Run: `bun run build`
Expected: PASS. If it fails to resolve `swiper/modules` or a `swiper/css/*` path, re-check
the import spelling against `node_modules/swiper/package.json` `exports`.

- [ ] **Step 4: Lint + format + test**

Run: `bun run lint && bun run format && bun run test`
Expected: lint exit 0; format clean (run `bun run format:fix` if not); 35 tests pass.

- [ ] **Step 5: Commit**

```bash
git add package.json bun.lock src/components/Testimonials.jsx
git commit -m "build: upgrade swiper to v14"
```

---

## Task 2: Verify the carousel

**Files:** none unless a fix is needed.

- [ ] **Step 1: Run the dev server and open the Testimonials section**

`bun run dev`, scroll to "Read reviews from Our Patients".

- [ ] **Step 2: Check behaviour**

- Autoplay advances a slide roughly every 4s.
- The pagination bullets below the slides render and clicking one jumps to that slide.
- The custom outlined prev/next circle buttons advance / rewind the carousel.
- Resizing the window across 768px and 1524px changes how many cards show (1 → 2 → 3).
- `loop` wraps: advancing past the last testimonial returns to the first with no blank gap.

- [ ] **Step 3: Check the console**

Filter for `Swiper`. Expected: no warnings. If there is a **"not enough slides for loop
mode"** warning or the loop visibly stutters, add `loopAdditionalSlides={2}` to the
`<Swiper>` element, rebuild, and re-check. Commit that separately:

```bash
git commit -am "fix: loopAdditionalSlides for swiper v14 loop mode"
```

- [ ] **Step 4: Compare the pagination bullets / arrows to before**

Swiper's default pagination-bullet and `--swiper-theme-color` styling changed slightly
between v9 and v14. If the bullets look wrong (wrong colour, too large), the fix is a
`--swiper-theme-color` / `--swiper-pagination-*` CSS variable in `src/index.css` — but only
if it actually looks off. Note any change in the PR.

- [ ] **Step 5: Screenshot the Testimonials section for the PR.** Stop the dev server.

---

## Task 3: Ship

- [ ] **Step 1: Final gates**

```bash
bun run format && bun run lint && bun run test && bun run build && bun install --frozen-lockfile
```

Expected: all PASS; frozen install reports no changes.

- [ ] **Step 2: Push + PR**

```bash
git push -u origin advisor/swiper-v14
```

PR against `master` using `.github/pull_request_template.md`. Summary: swiper 9 → 14, one
import-path change, carousel verified. Attach the screenshot.

- [ ] **Step 3: Confirm CI + Vercel preview are green.**

---

## Self-review notes

- No test covers the carousel, so `bun run test` only guards against collateral damage. The
  real verification is Task 2's manual check — do not skip it.
- If `bun add swiper@latest` also drags in a new transitive dep that `bun audit` flags, note
  it in the PR; `bun audit` is `continue-on-error` in CI so it will not block.

---

## As-built

- **One-line change** as planned: `import { Autoplay, Navigation, Pagination } from "swiper/modules"`.
  Everything else (`swiper/react`, the `swiper/css*` imports, all `<Swiper>` props) unchanged.
- **No `loopAdditionalSlides` needed** — no "not enough slides for loop mode" warning; loop
  mode initialised cleanly with the 6 testimonials.
- **No CSS-variable overrides** — pagination bullets render as Swiper's default `#007aff`
  8×8px, identical to v9.
- **Verified**: build passes; the Swiper instance mounts with the right config
  (`loop`, `autoplay` running, `slidesLength: 6`, `snapGrid: [0, 626, 1252, 1878, 2504]`,
  breakpoints applying `slidesPerView: 2` at ~1376px); slides are sized correctly (~606px
  at 2-up); pagination + the custom `.prev-button` / `.next-button` are present and bound;
  `slideNext()` advances; **zero console warnings/errors**. The Testimonials section renders
  identically to before (2-up cards, avatars, quotes, star ratings).
- **Automation-browser caveat**: this session's Chrome tab froze CSS transitions, so
  Swiper's initial autoplay transition never emitted `transitionend` and the instance sat in
  `animating: true`, blocking further moves until the flag was cleared manually. This is the
  same frozen-transition artifact seen with the announcement bar and Tailwind transitions
  earlier — it does not reproduce in a normal browser. Autoplay/nav behaviour should be
  eyeballed on the Vercel preview.
